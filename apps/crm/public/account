<!doctype html>
<html lang="ru" data-theme="dark">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
  <title>ABQD — Личный кабинет</title>

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700;800&display=swap" rel="stylesheet">

  <!-- Можно использовать общий стиль ABQD -->
  <link rel="stylesheet" href="/assets/app.css?v=3">

  <style>
    :root{
      --font: "Montserrat", ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Arial;
      --panelR: 18px;
      --txt: rgba(255,255,255,.92);
      --muted: rgba(255,255,255,.62);
      --line: rgba(255,255,255,.14);
      --shadow: 0 22px 80px rgba(0,0,0,.45);
      --accent: 46,124,255;
    }

    html[data-theme="soft"]{
      --txt: rgba(16,19,26,.92);
      --muted: rgba(16,19,26,.58);
      --line: rgba(16,19,26,.16);
      --shadow: 0 18px 60px rgba(18,24,40,.12);
      background:#f4f6fb;
    }

    *{box-sizing:border-box}
    body{
      margin:0;
      font-family:var(--font);
      color:var(--txt);
      min-height:100vh;
      background:
        radial-gradient(1100px 700px at 15% 10%, rgba(109,92,255,.14), transparent 55%),
        radial-gradient(1100px 700px at 85% 15%, rgba(255,77,157,.10), transparent 55%),
        #07090d;
    }
    html[data-theme="soft"] body{
      background:
        radial-gradient(1100px 700px at 15% 10%, rgba(109,92,255,.10), transparent 55%),
        radial-gradient(1100px 700px at 85% 15%, rgba(255,77,157,.08), transparent 55%),
        #f4f6fb;
    }

    .wrap{max-width:980px;margin:0 auto;padding:22px 14px 42px;}

    .top{
      display:flex;align-items:center;justify-content:space-between;gap:12px;flex-wrap:wrap;
      margin:6px 0 14px;
    }
    .brand{
      display:flex;align-items:center;gap:10px;
      font-weight:900;letter-spacing:-.02em;
      text-decoration:none;color:var(--txt);
    }
    .brandDot{
      width:10px;height:10px;border-radius:999px;
      background:linear-gradient(180deg, rgba(var(--accent),.95), rgba(var(--accent),.35));
      box-shadow:0 0 22px rgba(var(--accent), .22);
    }

    .actions{display:flex;gap:10px;flex-wrap:wrap;align-items:center}

    .miniBtn{
      border:1px solid var(--line);
      background:rgba(0,0,0,.18);
      color:var(--txt);
      padding:10px 12px;
      border-radius:999px;
      font-weight:800;
      cursor:pointer;
      text-decoration:none;
      font-size:13px;
    }
    html[data-theme="soft"] .miniBtn{background:rgba(255,255,255,.55)}
    .miniBtn:hover{filter:brightness(1.06)}

    .seg{
      display:inline-flex;border:1px solid var(--line);
      border-radius:999px;overflow:hidden;
      background:rgba(0,0,0,.18);
    }
    html[data-theme="soft"] .seg{background:rgba(255,255,255,.55)}
    .seg button{
      border:0;background:transparent;color:var(--muted);
      padding:10px 12px;font-weight:900;font-size:12px;cursor:pointer;
    }
    .seg button.active{color:var(--txt);background:rgba(255,255,255,.08)}
    html[data-theme="soft"] .seg button.active{background:rgba(16,19,26,.06)}

    h1{margin:14px 0 6px;font-size:34px;letter-spacing:-.04em;line-height:1.05}
    .help{margin:0;color:var(--muted);font-weight:700;max-width:780px;line-height:1.5}

    .card{
      margin-top:18px;
      border:1px solid var(--line);
      border-radius:var(--panelR);
      background:
        radial-gradient(900px 560px at 20% 0%, rgba(255,255,255,.10), transparent 60%),
        linear-gradient(180deg, rgba(255,255,255,.06), rgba(0,0,0,.14));
      box-shadow:var(--shadow);
      padding:18px;
      backdrop-filter: blur(14px);
      -webkit-backdrop-filter: blur(14px);
    }
    html[data-theme="soft"] .card{
      background:rgba(255,255,255,.65);
    }

    .grid{
      display:grid;
      grid-template-columns: 1fr 1fr;
      gap:14px;
    }
    @media (max-width: 820px){ .grid{grid-template-columns:1fr} }

    label{display:block;font-weight:900;margin:12px 0 8px;color:var(--muted)}
    input, textarea{
      width:100%;
      padding:14px 14px;
      border-radius:14px;
      border:1px solid var(--line);
      outline:none;
      font-family:var(--font);
      color:var(--txt);
      background:rgba(0,0,0,.18);
      font-size:15px;
    }
    html[data-theme="soft"] input, html[data-theme="soft"] textarea{
      background:rgba(255,255,255,.75);
    }
    textarea{min-height:120px;resize:vertical}

    .row{display:flex;gap:12px;flex-wrap:wrap;align-items:center;margin-top:14px}
    .btn{
      border:0;
      border-radius:16px;
      padding:14px 16px;
      font-weight:900;
      cursor:pointer;
      color:white;
      background:linear-gradient(180deg, rgba(var(--accent),.70), rgba(var(--accent),.28));
      border:1px solid rgba(var(--accent),.36);
      box-shadow:0 18px 60px rgba(0,0,0,.25);
      min-width:200px;
    }
    html[data-theme="soft"] .btn{color:rgba(16,19,26,.92)}
    .btn:disabled{opacity:.55;cursor:not-allowed}

    .status{
      font-weight:800;
      color:var(--muted);
    }

    .warn{
      margin-top:10px;
      color:var(--muted);
      font-weight:700;
      font-size:13px;
      line-height:1.45;
    }

    .danger{
      color: rgba(255,86,110,.92);
      font-weight:900;
    }
  </style>
</head>

<body>
  <div class="wrap">
    <div class="top">
      <a class="brand" href="/dashboard/"><span class="brandDot"></span> ABQD</a>

      <div class="actions">
        <a class="miniBtn" href="/dashboard/">CRM</a>
        <a class="miniBtn" href="/constructor/">Конструктор</a>
        <a class="miniBtn" href="/tariffs/">Тарифы</a>

        <div class="seg" id="themeSeg" aria-label="Тема">
          <button type="button" data-theme="dark" class="active">Тёмная</button>
          <button type="button" data-theme="soft">Белая</button>
        </div>

        <button class="miniBtn" id="btnLogout" type="button">Выйти</button>
      </div>
    </div>

    <h1>Личный кабинет</h1>
    <p class="help">
      Здесь ты меняешь данные профиля (то, что увидят люди на публичной странице).
      Сохраняем через API профиля.
    </p>

    <section class="card">
      <div class="grid">
        <div>
          <label for="fullName">Имя / ФИО</label>
          <input id="fullName" placeholder="Александр Неволин" />
        </div>
        <div>
          <label for="role">Роль / должность</label>
          <input id="role" placeholder="Директор / Основатель" />
        </div>

        <div>
          <label for="phone">Телефон</label>
          <input id="phone" placeholder="+7 999 000-12-34" />
        </div>

        <div>
          <label for="logoLink">Сайт</label>
          <input id="logoLink" placeholder="https://abqd.ru" />
        </div>

        <div style="grid-column:1/-1">
          <label for="about">О себе</label>
          <textarea id="about" placeholder="Коротко: кто ты, чем полезен, какие услуги..."></textarea>
        </div>
      </div>

      <div class="row">
        <button class="btn" id="btnSave" type="button">Сохранить</button>
        <div class="status" id="status">Загрузка…</div>
      </div>

      <div class="warn">
        Если токен отсутствует — страница отправит на авторизацию.
        Если у пользователя несколько профилей — этот кабинет обновляет “основной” slug из <code>/auth/me</code>.
      </div>
      <div class="warn danger" id="errBox" style="display:none;"></div>
    </section>
  </div>

  <script src="/dashboard/assets/account.js?v=1"></script>
</body>
</html>
